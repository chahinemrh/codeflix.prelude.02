module.exports = function intersection(...arrays){
    let newArr = []
    const first_tab = arrays // OU arrays.shift()
    for(const elt of first){
         let present = true
         for (const current of rest){
             if(!arrays[i].includes(elt)){
                 present = false
             }
         }
         if(present){
             newArr.push
         }
    }
 }